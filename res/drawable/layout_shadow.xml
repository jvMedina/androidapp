<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- Layer 0 -->
    <!-- Layer 1 -->
    <!-- Layer 2 -->


    <!-- dropshadow -->
    <!--
    <item>
        <shape>
            <gradient
                android:angle="180"
                android:centerColor="@color/shadow_end"
                android:endColor="@color/shadow_end"
                android:startColor="@color/shadow_end" />

            <padding
                android:bottom="2dp"
                android:left="0dp"
                android:right="0dp"
                android:top="0dp" />
        </shape>
    </item>
    -->
    <item>
        <shape>
            <gradient
                android:angle="180"
                android:endColor="@color/shadow_end"
                android:startColor="@color/shadow_end" />

            <padding
                android:bottom="3dp"
                android:left="0dp"
                android:right="0dp"
                android:top="0dp" />
        </shape>
    </item>

    <!-- content background -->
    <item>
        <shape>
            <solid android:color="@color/background" />
        </shape>
    </item>

</layer-list>